{
  "name": "generative-art",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "xo",
    "clean": "rimraf dist",
    "build": "yarn run clean && gulp",
    "dev": "yarn run build && gulp watch",
    "start": "serve dist"
  },
  "devDependencies": {
    "babel-preset-env": "^1.4.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-concat": "^2.6.1",
    "gulp-inject": "^4.2.0",
    "gulp-insert": "^0.5.0",
    "gulp-sass": "^3.1.0",
    "merge-stream": "^1.0.1",
    "rimraf": "^2.6.1",
    "serve": "^5.1.5",
    "xo": "^0.18.2"
  },
  "xo": {
    "semicolon": false,
    "space": 2,
    "rules": {
      "no-else-return": 0
    }
  }
}
